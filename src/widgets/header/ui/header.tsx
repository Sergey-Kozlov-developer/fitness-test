import { Timer } from "@features/timer";

export const Header = () => {
    return (
        <div className="sticky-nav bg-header">
            <div className="container mx-auto">{/* <Timer /> */}</div>
        </div>
    );
};
